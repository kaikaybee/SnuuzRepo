<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@drawable/gradient_background">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:theme="@style/Theme3"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <EditText
        android:id="@+id/editTime"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="@string/enter_wake_eup"
        app:fontFamily="@font/bebasneuebook"/>
    <Button
        android:id="@+id/wake_up_timeDialog"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="#008660D0"
        android:textColor="#ffffff"
        android:text="@string/dismiss_wake_up"
        android:textSize="40dp"
        app:fontFamily="@font/bebasneuebook"/>

    <Button
        android:id="@+id/SnoozeBtn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="#008660D0"
        android:text="@string/snooze"
        android:textColor="#ffffff"
        android:textSize="40dp"
        app:fontFamily="@font/bebasneuebook" />

</LinearLayout>
